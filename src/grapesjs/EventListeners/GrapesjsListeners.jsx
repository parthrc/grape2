const AddEventListeners = (editor) => {
  //   editor.on("component:update", (a) => {
  //     console.log("Component updated=", a);
  //   });
  //   editor.on("component:remove", (a) => {
  //     console.log(a);
  //   });
};

export default AddEventListeners;
