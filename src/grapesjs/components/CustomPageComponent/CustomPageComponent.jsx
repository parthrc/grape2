const CustomPageComponent = ({ content }) => {
  return (
    <div
      style={{
        height: "5rem",
      }}
    >
      Custom page {content}
    </div>
  );
};

export default CustomPageComponent;
